<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/images/coffee.svg" type="image/x-icon">
    <title>Inventory Site: <%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <% if(selected === 'home') { %> <link rel='stylesheet' href='/stylesheets/home.css' /> <% } %>
    <% if(selected === 'items') { %> <link rel='stylesheet' href='/stylesheets/items.css' /> <% } %>
    <% if(selected === 'categories') { %> <link rel='stylesheet' href='/stylesheets/categories.css' /> <% } %>
  </head>
  <body>
    <div class="content">
        <div class="menu">
            <div class="title"><img src="/images/coffee.svg" /> <span>Inventory</span></div>
            <div class="navigation">
                <ul>
                    <li onclick="location.href='/';" <% if(selected === 'home') { %> class="selected" <% } %>><img src="/images/home.svg" /><span>Home</span></li>
                    <li onclick="location.href='/items';"<% if(selected === 'items') { %> class="selected" <% } %>><img src="/images/archive.svg" /><span>Items</span></li>
                    <li onclick="location.href='/categories';"<% if(selected === 'categories') { %> class="selected" <% } %>><img src="/images/layers.svg" /><span>Categories</span></li>
                </ul>
            </div>
        </div>
        <div class="section-content">
            <%- layoutContent %>
        </div>
        <div class="section-footer">This Inventory Application was made for The Odin Project</div>
    </div>
  </body>
</html>
